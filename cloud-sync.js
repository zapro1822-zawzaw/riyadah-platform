(()=>{
  const URL='https://bnqbavlkvrpcbhqxctda.supabase.co';
  const KEY='sb_publishable_VdXBsOFWR1Y6N7GNm8lh_g_0Vl7319q';
  const sb=window.supabase?.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true}});
  const DATA_KEY='riyadah_v14_ultimate';
  let busy=false, schoolId=null, last='';
  const idOf=(x,p='item')=>String(x?.id||x?.key||x?.code||x?.source_key||`${p}:${JSON.stringify(x).slice(0,180)}`);
  const text=(x,...keys)=>{for(const k of keys){if(x&&x[k]!=null&&String(x[k]).trim()!=='')return String(x[k]).trim()}return ''};
  const arr=(x,...keys)=>{for(const k of keys)if(Array.isArray(x?.[k]))return x[k];return []};
  async function ensureWorkspace(user){
    const {data:m,error:me}=await sb.from('school_memberships').select('school_id,role,status').eq('user_id',user.id).eq('status','active').limit(1).maybeSingle();
    if(me)throw me;
    if(m?.school_id){schoolId=m.school_id;return schoolId;}
    const {data:p,error:pe}=await sb.from('profiles').select('full_name,school_name').eq('id',user.id).single();
    if(pe)throw pe;
    const name=text(p,'school_name')||`${text(p,'full_name')||user.email} — فضاء شخصي`;
    const {data:s,error:se}=await sb.from('schools').insert({name,created_by:user.id}).select('id').single();
    if(se){const {data:again,error:ae}=await sb.from('school_memberships').select('school_id').eq('user_id',user.id).eq('status','active').limit(1).maybeSingle();if(ae||!again)throw se;schoolId=again.school_id;return schoolId;}
    const {error:mi}=await sb.from('school_memberships').insert({school_id:s.id,user_id:user.id,role:'teacher',status:'active'});
    if(mi)throw mi;schoolId=s.id;return schoolId;
  }
  async function upsertLevels(user,data){const levels=arr(data,'levels','niveaux');for(let i=0;i<levels.length;i++){const x=levels[i]||{};const name=text(x,'name','title','label')||`المستوى ${i+1}`;const key=idOf(x,`level:${i}`);await sb.from('levels').upsert({school_id:schoolId,source_key:key,name,code:text(x,'code'),sort_order:i,created_by:user.id},{onConflict:'school_id,source_key'});}}
  async function upsertSubjects(user,data){const subjects=arr(data,'subjects','matieres','materials');for(let i=0;i<subjects.length;i++){const x=subjects[i]||{};const name=text(x,'name','title','label')||String(x||'');if(!name)continue;const key=idOf(x,`subject:${i}`);await sb.from('subjects').upsert({school_id:schoolId,source_key:key,name,code:text(x,'code')},{onConflict:'school_id,source_key'});}}
  async function upsertClasses(user,data){const levels=(await sb.from('levels').select('id,source_key').eq('school_id',schoolId)).data||[];const lm=new Map(levels.map(x=>[x.source_key,x.id]));const classes=arr(data,'classes','groups','sections','afwaj');for(let i=0;i<classes.length;i++){const x=classes[i]||{};const key=idOf(x,`class:${i}`);const name=text(x,'name','title','label')||`قسم ${i+1}`;const lk=text(x,'levelId','level_id','levelKey');const level_id=lk?lm.get(lk)||null:null;await sb.from('classes').upsert({school_id:schoolId,source_key:key,name,level_id,teacher_id:user.id,school_year:text(x,'year','school_year')||'2026/2027'},{onConflict:'school_id,source_key'});}}
  async function upsertStudents(user,data){const classes=(await sb.from('classes').select('id,source_key').eq('school_id',schoolId)).data||[];const cm=new Map(classes.map(x=>[x.source_key,x.id]));const students=arr(data,'students','learners','eleves','learnersList');for(let i=0;i<students.length;i++){const x=students[i]||{};const key=idOf(x,`student:${i}`);const full=text(x,'name','full_name','fullName');const first=text(x,'first_name','firstName')||full.split(/\s+/)[0]||`متعلم ${i+1}`;const last=text(x,'last_name','lastName')||full.split(/\s+/).slice(1).join(' ')||' ';const ck=text(x,'classId','class_id','classKey');await sb.from('students').upsert({school_id:schoolId,source_key:key,first_name:first,last_name:last,student_code:text(x,'code','student_code','id'),class_id:ck?cm.get(ck)||null:null,gender:text(x,'gender','sexe'),notes:text(x,'notes','note')},{onConflict:'school_id,source_key'});}}
  async function pushSpecial(user,data){const mind=arr(data,'mindmaps','mindMaps','maps');for(let i=0;i<mind.length;i++){const x=mind[i]||{};await sb.from('mindmaps').upsert({school_id:schoolId,source_key:idOf(x,`mindmap:${i}`),title:text(x,'title','name')||`خريطة ${i+1}`,source_filename:text(x,'source_filename','filename','fileName'),source_language:text(x,'source_language','language','lang'),structure:x.structure||x.tree||x},{onConflict:'school_id,source_key'});}const flu=arr(data,'fluency','fluencyRecords');for(let i=0;i<flu.length;i++){const x=flu[i]||{};await sb.from('fluency_records').upsert({school_id:schoolId,source_key:idOf(x,`fluency:${i}`),class_id:null,student_id:null,level:text(x,'level'),words_per_minute:Number(x.words_per_minute??x.wpm)||null,accuracy:Number(x.accuracy)||null,mastery:text(x,'mastery','status'),created_by:user.id},{onConflict:'school_id,source_key'});}}
  async function normalize(user,data){await ensureWorkspace(user);await upsertLevels(user,data);await upsertSubjects(user,data);await upsertClasses(user,data);await upsertStudents(user,data);await pushSpecial(user,data);}
  async function tick(){if(busy||!sb)return;const {data:{session}}=await sb.auth.getSession();if(!session)return;const frame=document.getElementById('appFrame');if(!frame)return;let raw=null;try{raw=frame.contentWindow.localStorage.getItem(DATA_KEY)}catch{}if(!raw||raw===last)return;busy=true;try{const data=JSON.parse(raw);await normalize(session.user,data);last=raw;document.title='رَوّاد — سحابي';}catch(e){console.warn('Cloud normalization:',e)}finally{busy=false}}
  sb?.auth.onAuthStateChange(()=>{last='';schoolId=null});
  setInterval(tick,3500);setTimeout(tick,1200);

  const adminCss=`<style id="riyadah-admin-css">#riyadah-admin-launch{position:fixed;right:18px;bottom:18px;z-index:99999;border:0;border-radius:14px;padding:12px 16px;background:#0b5ed7;color:white;font-weight:900;box-shadow:0 8px 30px #0003;cursor:pointer;font-family:inherit}#riyadah-admin-overlay{position:fixed;inset:0;z-index:100000;background:#0008;display:none;align-items:center;justify-content:center;padding:18px;font-family:system-ui,-apple-system,"Segoe UI",Tahoma,Arial,sans-serif}#riyadah-admin-modal{width:min(1050px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:22px;padding:20px;direction:rtl;box-shadow:0 25px 80px #0008}#riyadah-admin-modal h2{margin:0 0 14px}.ra-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:10px;margin-bottom:18px}.ra-card{background:#f6f9fc;border:1px solid #e1e8f0;border-radius:14px;padding:14px}.ra-card b{display:block;font-size:24px}.ra-toolbar{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0}.ra-toolbar input,.ra-toolbar select{padding:10px;border:1px solid #cbd5df;border-radius:10px;font:inherit}.ra-table{width:100%;border-collapse:collapse;font-size:14px}.ra-table th,.ra-table td{padding:9px;border-bottom:1px solid #e7edf3;text-align:right;vertical-align:middle}.ra-table th{background:#f4f7fa}.ra-btn{border:0;border-radius:8px;padding:7px 10px;font:inherit;font-weight:800;cursor:pointer;margin:2px}.ra-ok{background:#dff7ec;color:#08734f}.ra-warn{background:#fff1d8;color:#8a5200}.ra-danger{background:#ffe1e1;color:#9b2020}.ra-close{float:left;border:0;background:#eef2f6;border-radius:10px;padding:8px 12px;cursor:pointer;font-weight:900}.ra-empty{padding:20px;text-align:center;color:#66788a}</style>`;
  async function adminData(){const [p,w,m]=await Promise.all([sb.from('profiles').select('id,full_name,role,requested_role,school_name,is_active,created_at,updated_at').order('created_at',{ascending:false}),sb.from('workspaces').select('id,name,created_by,created_at').order('created_at',{ascending:false}),sb.from('workspace_members').select('workspace_id,user_id,role,status,created_at,updated_at')]);if(p.error)throw p.error;if(w.error)throw w.error;if(m.error)throw m.error;return {profiles:p.data||[],workspaces:w.data||[],members:m.data||[]}}
  async function setProfile(id,patch){const {error}=await sb.from('profiles').update(patch).eq('id',id);if(error)throw error}
  async function setMembership(workspace_id,user_id,patch){const {error}=await sb.from('workspace_members').update(patch).eq('workspace_id',workspace_id).eq('user_id',user_id);if(error)throw error}
  function roleLabel(r){return r==='admin'?'مدير':r==='inspector'?'مفتش':'أستاذ'}
  async function renderAdmin(){const root=document.getElementById('riyadah-admin-modal');if(!root)return;root.innerHTML='<div class="ra-empty">جاري تحميل بيانات الإدارة…</div>';try{const d=await adminData();const pending=d.profiles.filter(p=>!p.is_active||p.requested_role==='inspector');root.innerHTML=`<button class="ra-close" id="ra-close">إغلاق</button><h2>🛡️ إدارة المنصة</h2><div class="ra-grid"><div class="ra-card">المستخدمون<b>${d.profiles.length}</b></div><div class="ra-card">طلبات تحتاج مراجعة<b>${pending.length}</b></div><div class="ra-card">المؤسسات/الفضاءات<b>${d.workspaces.length}</b></div><div class="ra-card">العضويات<b>${d.members.length}</b></div></div><div class="ra-toolbar"><input id="ra-search" placeholder="بحث بالاسم أو المؤسسة أو الدور"><select id="ra-filter"><option value="all">كل المستخدمين</option><option value="pending">طلبات/غير مفعلة</option><option value="teacher">أساتذة</option><option value="inspector">مفتشون</option><option value="admin">مديرون</option></select><button class="ra-btn ra-ok" id="ra-refresh">↻ تحديث</button></div><div id="ra-body"></div>`;document.getElementById('ra-close').onclick=()=>{root.parentElement.style.display='none'};document.getElementById('ra-refresh').onclick=renderAdmin;const body=document.getElementById('ra-body');const draw=()=>{const q=document.getElementById('ra-search').value.trim().toLowerCase(),f=document.getElementById('ra-filter').value;const rows=d.profiles.filter(p=>(f==='all'||(f==='pending'&&(!p.is_active||p.requested_role==='inspector'))||p.role===f)&&(!q||[p.full_name,p.school_name,p.role,p.requested_role].some(v=>String(v||'').toLowerCase().includes(q))));body.innerHTML=rows.length?`<table class="ra-table"><thead><tr><th>المستخدم</th><th>الصفة</th><th>المؤسسة</th><th>الحالة</th><th>الإجراء</th></tr></thead><tbody>${rows.map(p=>`<tr><td><b>${p.full_name||'بدون اسم'}</b><br><small>${p.id.slice(0,8)}…</small></td><td>${roleLabel(p.role)}${p.requested_role&&p.requested_role!==p.role?`<br><small>مطلوب: ${roleLabel(p.requested_role)}</small>`:''}</td><td>${p.school_name||'—'}</td><td>${p.is_active?'🟢 نشط':'🟠 في انتظار التفعيل'}</td><td>${p.id!==d.profiles.find(x=>x.role==='admin')?.id?`<button class="ra-btn ra-ok" data-act="activate" data-id="${p.id}">تفعيل</button><button class="ra-btn ra-warn" data-act="teacher" data-id="${p.id}">أستاذ</button><button class="ra-btn ra-warn" data-act="inspector" data-id="${p.id}">مفتش</button><button class="ra-btn ra-danger" data-act="deactivate" data-id="${p.id}">تعليق</button>`:'حساب الإدارة'}</td></tr>`).join('')}</tbody></table>`:'<div class="ra-empty">لا توجد نتائج.</div>';body.querySelectorAll('[data-act]').forEach(b=>b.onclick=async()=>{const id=b.dataset.id,act=b.dataset.act;b.disabled=true;try{if(act==='activate')await setProfile(id,{is_active:true});if(act==='deactivate')await setProfile(id,{is_active:false});if(act==='teacher')await setProfile(id,{role:'teacher',requested_role:'teacher',is_active:true});if(act==='inspector')await setProfile(id,{role:'inspector',requested_role:'inspector',is_active:true});await renderAdmin()}catch(e){alert('تعذر تنفيذ العملية: '+(e.message||e))}})};document.getElementById('ra-search').oninput=draw;document.getElementById('ra-filter').onchange=draw;draw()}catch(e){root.innerHTML=`<div class="ra-empty">تعذر تحميل لوحة الإدارة.<br>${e.message||e}</div>`}}
  function mountAdmin(){const frame=document.getElementById('appFrame');if(!frame||!frame.contentDocument)return;sb.auth.getSession().then(async({data:{session}})=>{if(!session)return;const {data:p}=await sb.from('profiles').select('role').eq('id',session.user.id).maybeSingle();if(p?.role!=='admin')return;const doc=frame.contentDocument;if(!doc.getElementById('riyadah-admin-css'))doc.head.insertAdjacentHTML('beforeend',adminCss);if(doc.getElementById('riyadah-admin-launch'))return;const btn=doc.createElement('button');btn.id='riyadah-admin-launch';btn.textContent='🛡️ الإدارة';doc.body.appendChild(btn);const ov=doc.createElement('div');ov.id='riyadah-admin-overlay';ov.innerHTML='<div id="riyadah-admin-modal"></div>';doc.body.appendChild(ov);btn.onclick=()=>{ov.style.display='flex';renderAdmin()};})}
  setInterval(mountAdmin,2200);
})();